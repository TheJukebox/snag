# snag
A simple, self-hosted solution for snagging media built with Sveltekit.

## Quickstart

You can spin up snag the latest version of snag in a docker container:

```bash
docker run -d -p 3000:3000 --name snag ghcr.io/thejukebox/snag:latest 
```

