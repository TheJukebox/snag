# snag
A simple, self-hosted solution for snagging media built with Sveltekit.

## Quickstart

You can spin up snag in a docker container:

```bash
docker build -t snag:latest .
docker run -d -p 3000:3000 --name snag snag:latest
```

